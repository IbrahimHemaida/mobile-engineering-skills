name: Validate Skills

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check SKILL.md files exist
        run: |
          test -f skills/mobile-architecture-guard/SKILL.md || exit 1
          test -f skills/mobile-tdd-guard/SKILL.md || exit 1
          test -f skills/ai-context-budget-guard/SKILL.md || exit 1
          test -f skills/mobile-security-guard/SKILL.md || exit 1
          test -f skills/mobile-accessibility-guard/SKILL.md || exit 1
          test -f skills/mobile-kmm-migration-guard/SKILL.md || exit 1
          echo "✓ All SKILL.md files exist"
      
      - name: Validate SKILL.md frontmatter
        run: |
          for skill in skills/*/SKILL.md; do
            echo "Checking $skill"
            grep -q "^name:" "$skill" || (echo "Missing 'name:' in $skill" && exit 1)
            grep -q "^description:" "$skill" || (echo "Missing 'description:' in $skill" && exit 1)
          done
          echo "✓ All SKILL.md files have valid frontmatter"
      
      - name: Check for broken reference links
        run: |
          fail=0
          for skill_dir in skills/*/; do
            skill_md="${skill_dir}SKILL.md"
            [ -f "$skill_md" ] || continue
            for ref in $(grep -oE 'references/[A-Za-z0-9_.-]+\.md' "$skill_md" | sort -u); do
              if [ ! -f "${skill_dir}${ref}" ]; then
                echo "❌ Broken reference in $skill_md: $ref"
                fail=1
              fi
            done
          done
          if [ "$fail" -eq 1 ]; then
            exit 1
          fi
          echo "✓ No broken reference links found"
          echo "✓ Reference files validated"
      
      - name: Ensure README.md exists
        run: |
          test -f README.md || exit 1
          echo "✓ README.md exists"
      
      - name: Ensure LICENSE exists
        run: |
          test -f LICENSE || exit 1
          echo "✓ LICENSE exists"

  spell-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Spell check README and SKILLs
        uses: codespell-project/actions-codespell@v2
        with:
          files: README.md skills/*/SKILL.md
          skip: ".git,node_modules"
          ignore_words_file: .codespellignore
