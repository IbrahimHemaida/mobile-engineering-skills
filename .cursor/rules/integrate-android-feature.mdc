---
description: Wire a scaffolded Android/Kotlin feature into the real app — navigation graph, app-level Hilt DI graph, Gradle dependency reconciliation, and a mandatory build-verification pass. Use after scaffold-android-feature, or to make a feature actually runnable.
globs: ["**/*.kt", "**/*.kts"]
alwaysApply: false
---

Read and follow `skills/integrate-android-feature/SKILL.md` (relative to the project root) for this integration. If your Cursor version supports `@` file mentions with relative paths, `@skills/integrate-android-feature/SKILL.md` also works — otherwise open the file directly.

This is a thin pointer, not a copy — the full integration workflow lives in the referenced file. See `skills/integrate-android-feature/references/android-integration-templates.md` for the exact navigation, Gradle, and multi-module wiring templates.
