---
description: Scaffold a complete Flutter feature module (Domain, Data, Presentation, GetIt/Injectable DI, full Unit + Widget tests) following Clean Architecture, BLoC, and the same 24 imperatives as mobile-architecture-guard. Use when creating a new Flutter feature or screen from scratch.
globs: ["**/*.dart"]
alwaysApply: false
---

Read and follow `skills/scaffold-flutter-feature/SKILL.md` (relative to the project root) to generate this feature. If your Cursor version supports `@` file mentions with relative paths, `@skills/scaffold-flutter-feature/SKILL.md` also works — otherwise open the file directly.

This is a thin pointer, not a copy — the full generation workflow and code templates live in the referenced file. See `skills/scaffold-flutter-feature/references/dart-templates.md` for the complete Domain/Data/Presentation/DI/test code templates.
