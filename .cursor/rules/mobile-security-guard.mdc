---
description: Review Kotlin/Android and Flutter code for security vulnerabilities before merge — secrets, certificate pinning, insecure storage, WebView/deep-link safety. Use for auth, storage, and networking code, or before a security audit.
globs: ["**/*.kt", "**/*.kts", "**/*.dart"]
alwaysApply: false
---

Read and follow `skills/mobile-security-guard/SKILL.md` (relative to the project root) for this review. If your Cursor version supports `@` file mentions with relative paths, `@skills/mobile-security-guard/SKILL.md` also works — otherwise open the file directly.

This is a thin pointer, not a copy — the full ruleset (21 imperatives and a severity-ranked findings report format) lives in the referenced file. See `skills/mobile-security-guard/references/` for extended certificate-pinning, secure-storage, and WebView/IPC guidance if a finding needs more context than the summary in SKILL.md provides.
