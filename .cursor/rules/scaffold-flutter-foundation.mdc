---
description: Scaffold the app-wide foundation a Flutter app needs before any feature is built — design system (colors, typography, spacing, dark mode, glass nav bar), localization, shared widget library, core network/data layer, central constants/environment config, responsive support, and hardened .gitignore. Use once, early in a project, before scaffold-flutter-feature.
globs: ["**/*.dart"]
alwaysApply: false
---

Read and follow `skills/scaffold-flutter-foundation/SKILL.md` (relative to the project root) to generate this foundation. If your Cursor version supports `@` file mentions with relative paths, `@skills/scaffold-flutter-foundation/SKILL.md` also works — otherwise open the file directly.

This is a thin pointer, not a copy — the full generation workflow lives in the referenced file. See `skills/scaffold-flutter-foundation/references/flutter-foundation-templates.md` for the complete design-system, localization, shared-widget, network, and constants code templates.
