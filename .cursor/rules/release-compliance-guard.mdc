---
description: Review an Android/Kotlin or Flutter app before store submission — verify permissions match actual feature usage, and check Apple App Store and Google Play requirements that commonly cause rejection (Privacy Manifest, ATT, Data Safety form, target SDK level, usage-description strings). Use before any production build upload.
globs: ["**/*.kt", "**/*.dart", "**/AndroidManifest.xml", "**/Info.plist"]
alwaysApply: false
---

Read and follow `skills/release-compliance-guard/SKILL.md` (relative to the project root) for this review. If your Cursor version supports `@` file mentions with relative paths, `@skills/release-compliance-guard/SKILL.md` also works — otherwise open the file directly.

This is a thin pointer, not a copy — the full review workflow lives in the referenced file. See `skills/release-compliance-guard/references/permission-mapping.md` and `references/store-requirements.md` for the complete permission-to-API mapping and store-specific requirements.
